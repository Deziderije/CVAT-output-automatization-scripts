Command:
	bash InfoExtracter.sh

Dependencies:
	mediainfo [sudo apt-get install mediainfo]

----------------------------------------------------------------------------------
Extracts info from every .mp4 or .avi file in the current folder and writes
it to results.txt in the following format:

Name Resolution FPS FrameCount
